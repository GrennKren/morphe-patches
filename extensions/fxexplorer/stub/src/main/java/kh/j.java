/*
 * Stub for FX Explorer's base item interface (Lkh/j;).
 * Used only at compile time. At runtime, the real class from the APK is used.
 */
package kh;

@SuppressWarnings("unused")
public interface j {
    String getName();
    Object getPath(); // Returns Lhh/f; — use toString() to get path string
    String w();       // From kh/e — returns MIME type
}
