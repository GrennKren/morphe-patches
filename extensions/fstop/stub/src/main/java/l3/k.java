/*
 * Stub class for F-Stop's data model (l3.k).
 * Used only at compile time so extension code can reference APK classes.
 *
 * Real DEX field names (verified via androguard):
 * - l3.k.a = ArrayList of c3.t media items (public)
 * - l3.k.o() = getCurrentItem -> c3.t
 */
package l3;

import c3.t;

@SuppressWarnings("unused")
public class k {

    public java.util.ArrayList a;  // list of c3.t media items

    public t o() {
        return null; // stub - getCurrentItem
    }
}
